import { Button } from "@/components/ui/button";
export const Header = () => {
  return <header className="fixed top-0 left-0 right-0 z-50 bg-vault-background/95 backdrop-blur-md border-b border-vault-text/10">
      
    </header>;
};